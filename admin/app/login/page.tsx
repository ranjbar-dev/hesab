"use client";
import Link from "next/link";import{useRouter}from"next/navigation";import{useState,type FormEvent}from"react";import{toast}from"sonner";import{apiFetch}from"@/lib/api";import{setAccessToken,setPending}from"@/lib/auth";
const normalize=(v:string)=>v.replace(/[\s()\-]/g,"").replace(/^\+98|^0098|^0/,"");
export default function Login(){const r=useRouter();const[phone,setPhone]=useState("");const[password,setPassword]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();const p=normalize(phone);if(!/^9\d{9}$/.test(p)){toast.error("شماره موبایل معتبر نیست");return}setLoading(true);try{const d=await apiFetch("/admin/auth/login",{method:"POST",body:{phone_number:p,password}});if(d.twofa_required){setPending(d.pending_token);toast.info("کد تأیید دو مرحله‌ای را وارد کنید");r.push("/login/2fa")}else{setAccessToken(d.access_token);toast.success("خوش آمدید");r.replace("/")}}catch(e){toast.error(e instanceof Error?e.message:"خطا")}finally{setLoading(false)}}return <Auth title="ورود به پنل مدیریت" text="برای ادامه، اطلاعات حساب مدیر را وارد کنید."><form onSubmit={submit} className="space-y-5"><Field label="شماره موبایل" value={phone} set={setPhone} auto="tel"/><Field label="رمز عبور" value={password} set={setPassword} type="password" auto="current-password"/><Link href="/forgot-password" className="block text-sm text-brand-accent hover:text-brand-accent-hover">رمز عبور را فراموش کرده‌اید؟</Link><Button loading={loading}>ورود</Button></form></Auth>}
export function Auth({title,text,children}:{title:string;text:string;children:React.ReactNode}){return <main className="min-h-screen grid lg:grid-cols-2"><section className="hidden lg:flex flex-col justify-between p-12 bg-gradient-to-bl from-brand-surface to-brand-bg border-l border-brand-border"><b className="text-xl text-brand-accent">حساب</b><div><h1 className="text-3xl font-bold leading-relaxed">مدیریت شرکت‌ها و اشتراک‌ها</h1><p className="mt-4 max-w-sm leading-8 text-brand-muted">پنل مرکزی برای کنترل حساب شرکت‌ها، پلن‌های اشتراک و صورتحساب‌های آنلاین.</p></div><small className="text-brand-muted">© ۱۴۰۴ حساب</small></section><section className="flex items-center justify-center p-6"><div className="w-full max-w-sm rounded-2xl border border-brand-border bg-brand-surface/50 p-6 sm:p-8"><h2 className="text-2xl font-bold">{title}</h2><p className="mt-2 mb-8 text-sm text-brand-muted">{text}</p>{children}</div></section></main>}
export function Field({label,value,set,type="tel",auto}:{label:string;value:string;set:(v:string)=>void;type?:string;auto?:string}){const id=label;return <div><label htmlFor={id} className="mb-2 block text-sm">{label}</label><input id={id} type={type} inputMode={type==="tel"?"numeric":undefined} value={value} onChange={e=>set(e.target.value)} autoComplete={auto} required className="h-11 w-full rounded-lg border border-brand-border bg-brand-bg px-3 text-sm outline-none transition-colors focus:border-brand-accent focus:ring-2 focus:ring-brand-accent/30"/></div>};export function Button({children,loading}:{children:React.ReactNode;loading?:boolean}){return <button disabled={loading} className="h-11 w-full cursor-pointer rounded-lg bg-brand-accent font-semibold text-brand-bg transition-colors hover:bg-brand-accent-hover disabled:opacity-60 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-accent">{loading?"در حال انجام…":children}</button>}
