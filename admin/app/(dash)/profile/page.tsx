"use client";
import Link from "next/link";
import { useRef, useState } from "react";
import { toast } from "sonner";
import FormField from "@/components/FormField";
import Select from "@/components/Select";
import { useAdmin } from "@/components/Sidebar";
import { API_BASE } from "@/lib/api";
import { deleteAvatar, updateProfile, uploadAvatar } from "@/lib/adminProfile";
export default function Profile() { const { admin, reload } = useAdmin(), input = useRef<HTMLInputElement>(null); const [form, setForm] = useState({ first_name: admin.first_name, last_name: admin.last_name, email: admin.email, phone_number: admin.phone_number, is_male: admin.is_male }); const [avatarVersion, setAvatarVersion] = useState(Date.now()), [busy, setBusy] = useState(false); const set = (k: keyof typeof form, value: string | boolean) => setForm((v) => ({ ...v, [k]: value })); async function save(e: React.FormEvent) { e.preventDefault(); setBusy(true); try { await updateProfile(form); await reload(); toast.success("پروفایل ذخیره شد"); } catch (e) { toast.error(e instanceof Error ? e.message : "خطا"); } finally { setBusy(false); } } async function choose(file?: File) { if (!file) return; try { await uploadAvatar(file); await reload(); setAvatarVersion(Date.now()); toast.success("تصویر پروفایل به‌روزرسانی شد"); } catch (e) { toast.error(e instanceof Error ? e.message : "خطا"); } finally { if (input.current) input.current.value = ""; } } async function remove() { try { await deleteAvatar(); await reload(); toast.success("تصویر پروفایل حذف شد"); } catch (e) { toast.error(e instanceof Error ? e.message : "خطا"); } } const initials = `${admin.first_name[0] ?? ""}${admin.last_name[0] ?? ""}`; return <section className="mx-auto max-w-2xl p-6 sm:p-12"><p className="text-sm text-brand-muted">حساب کاربری</p><h1 className="mt-1 text-3xl font-bold">پروفایل</h1><div className="mt-8 flex items-center gap-4 rounded-2xl border border-brand-border bg-brand-surface p-5">{admin.avatar_url ? <img src={`${API_BASE}${admin.avatar_url}?v=${avatarVersion}`} alt="تصویر پروفایل" className="h-20 w-20 rounded-full object-cover" /> : <span className="grid h-20 w-20 place-items-center rounded-full bg-brand-accent text-xl font-bold text-brand-bg">{initials}</span>}<div><input ref={input} type="file" accept="image/png,image/jpeg,image/webp" onChange={(e) => choose(e.target.files?.[0])} className="sr-only" /><button type="button" onClick={() => input.current?.click()} className="cursor-pointer rounded-lg bg-brand-accent px-4 py-2 font-bold text-brand-bg">انتخاب تصویر</button>{admin.avatar_url && <button type="button" onClick={remove} className="mr-3 cursor-pointer text-sm text-red-400">حذف تصویر</button>}<p className="mt-2 text-xs text-brand-muted">PNG، JPEG یا WebP تا ۱ مگابایت</p></div></div><form onSubmit={save} className="mt-5 space-y-4 rounded-2xl border border-brand-border bg-brand-surface p-6"><div className="grid gap-4 sm:grid-cols-2"><FormField label="نام" value={form.first_name} onChange={(v) => set("first_name", v)} placeholder="مثال: احمد" /><FormField label="نام خانوادگی" value={form.last_name} onChange={(v) => set("last_name", v)} placeholder="مثال: رضایی" /></div><FormField label="ایمیل" type="email" required={false} value={form.email} onChange={(v) => set("email", v)} placeholder="example@mail.com" /><FormField label="موبایل" type="tel" value={form.phone_number} onChange={(v) => set("phone_number", v)} placeholder="۰۹۱۲۱۲۳۴۵۶۷" /><label className="block text-sm">جنسیت<Select value={{ value: String(form.is_male), label: form.is_male ? "مرد" : "زن" }} onChange={(v) => set("is_male", v?.value === "true")} options={[{ value: "true", label: "مرد" }, { value: "false", label: "زن" }]} placeholder="جنسیت را انتخاب کنید" /></label><button disabled={busy} className="h-11 w-full cursor-pointer rounded-lg bg-brand-accent font-bold text-brand-bg disabled:opacity-60">{busy ? "در حال ذخیره…" : "ذخیره تغییرات"}</button></form><Link href="/settings/security" className="mt-5 inline-block text-sm text-brand-accent">تنظیمات امنیتی و رمز عبور ←</Link></section>; }
