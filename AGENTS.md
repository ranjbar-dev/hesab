# AGENTS.md

All agent instructions live in [`./CLAUDE.md`](./CLAUDE.md). Read it fully
before acting.

**Learnings rule:** when the user supplies a fix or change that must persist
across sessions, append a dated bullet to `CLAUDE.md` (or the nearest folder
`CLAUDE.md`) so the same mistake is not repeated.
