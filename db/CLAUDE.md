# ./db — Database

See root [`../CLAUDE.md`](../CLAUDE.md) for product, workflow, and always-on
tools.

## Rules

- `./postgres` — Postgres Docker image config, init SQL, tuning params.
- Schema migrations are owned by `./api` (sqlc workflow) — see
  [`../api/CLAUDE.md`](../api/CLAUDE.md).
- This folder is infra config only, no application code.

## Learnings

<!-- LEARNINGS -->
- _(none yet)_
